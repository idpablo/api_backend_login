name: Portifolio CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: |
           docker build -t mondodb -f Dockerfile .
           docker run -it --rm -p 27017:27017 --name mongodb_container -t mondodb


     - name: Docker Run
        # You may pin to the exact commit or the version.
        # uses: tj-actions/docker-run@1d25a677da8f1243a138dc5db21e307e92986ffe
        uses: tj-actions/docker-run@v2.2.1
        with:
          # Docker image
          image: 
          # Container name
          name: 
          # Command to run
          args: # optional
          # Options to pass to docker run
          options: # optional, default is 
          
